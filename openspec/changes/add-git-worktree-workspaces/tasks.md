## 1. Mock-backed actual workspace frontend first

- [x] 1.15 Implement selected A Active groups as the sole worktree-capable dashboard layout through reusable actual presentation. Remove the alternate renderer/toggle, retain test-only scenario controls, safely canonicalize old links and preserve no-capability behavior. Verify independent lifecycle, default/operation/reload grouping, compact flows, full/dedicated typechecks, targeted unit and desktop/touch regressions. Run Active groups gallery last, refresh existing secured demo and gallery memory processes without proxy changes, and record current evidence/results. Gate 2.2 remains unchecked.

User decision (2026-09-17): "Lets go on A" selects Active groups and supersedes the comparison below. This is layout approval only, not final UX approval or authorization for real backend integration. The 1.14 comparison and counts below are historical evidence, not current review instructions.

Revision 1.15 verification (2026-09-17): full/dedicated typechecks passed; 161 targeted tests (1188 assertions) and 82 desktop/touch E2E tests (1.4 minutes, four workers) passed. Default Active groups, obsolete-link canonicalization, independent lifecycle, keyboard disclosures, create/delete/reload presentation, initial local/remote branch clicks and legacy no-capability behavior passed. Gallery ran last: one test passed (44.5 seconds; 46.1 seconds total), producing 80 images with only selected-layout evidence, including 18 actual localhost/tailnet captures. Inspected representative mixed/all-stopped, inheritance and compact phone-delete screenshots. Refreshed existing secured loopback demo and gallery memory snapshot without proxy/Tailscale changes; both origins returned 200, obsolete-link browser canonicalized with no toggle, running-child SPA loaded, wrong-Origin mutation returned 403, gallery POST 405/unknown 404, and gallery image loaded without browser errors. Live demo reset to five-row mixed-lifecycle. Current walkthrough/results: `tests/e2e/WORKTREE-DEMO.md`. OpenSpec validation and diff checks passed. Gate 2.2 stays unchecked; no real integration authorized or implemented.

- [x] 1.14 Compare both provisional dashboard alternatives against the same mock state: A Active groups (initial demo only) and B Running shortcuts plus complete stable hierarchy. Separate repository configuration headings from main checkout lifecycle rows; main Stop affects only main and children can start with main stopped. Preserve original rows/actions, exact branches/provenance, compact creation/deletion and initial local/remote branch clicks without typing. Keep layout/scenario controls test-only and layout choice through operations/reload without state reset. Verify mixed/all-stopped classification, create/delete/start/stop, desktop/touch keyboard disclosures and toggle, full/dedicated typechecks, targeted regressions and E2E. Capture matching A/B desktop/touch mixed/all-stopped evidence; run gallery last, refresh its memory snapshot and existing secured demo endpoints, update review walkthrough. Neither candidate is selected and 2.2 remains unchecked.

Earlier comparison decision (superseded by 1.15): independent checkout lifecycle was approved and both alternatives were requested for comparison. Parent is the configuration owner, not a prerequisite running session. Task 2.2 remains unapproved after the subsequent layout selection.

Revision 1.14 verification (2026-09-17): full/dedicated typechecks passed; 161 targeted tests (1181 assertions), 84 desktop/touch E2E tests (2.4 minutes) passed. Initial local/remote option clicks without typing passed on dashboard and selector; no picker code change was needed. Final gallery test ran last and passed in 46.9 seconds, producing 86 images including eight matching A/B desktop/touch mixed/all-stopped captures and actual localhost/tailnet A/B. Inspected matching images and served phone dialogs. Restarted existing loopback demo and read-only gallery memory snapshot with their unchanged exact HTTPS origins. Both A/B endpoints and fresh gallery images returned 200; running-child SPA and gallery browser had no page errors, wrong-Origin mutation 403, gallery POST 405/unknown 404. Live demo is left in mixed-lifecycle (five rows, main stopped + child running); stopped main URL truthfully returns 409 until Start. Review walkthrough/results: `tests/e2e/WORKTREE-DEMO.md`. No Tailscale changes, real operations or UX approval.

- [x] 1.13 Implement and reverify the approved compact completion/removal revision on every mock creation pathway: close successful creation, refresh source lists without switching, show `Created <branch>` with explicit start-then-Open; keep retained-registration and async failures compact with retry. Replace metadata-heavy deletion with parent/branch identity, exact stopped/running consequences and Cancel plus Delete/Stop and delete, no extra checkbox or simulation banner inside. Preserve every safety recheck, blockers and branches. Verify desktop/phone dialog closure, explicit navigation, list refresh, labels, stop failure and bounds; full/dedicated typechecks and targeted regressions; refresh and inspect localhost/tailnet demo evidence, then run the revised desktop gallery last and refresh its existing server. Record actual results and limitations; task 2.2 remains unchecked.

Revision 1.13 supersedes historical exact-path/checkbox deletion and post-create result/Details navigation in earlier completed tasks. The approved identity is parent display name / branch; hidden stable identity remains an operation concern, not dialog content. No real integration or final UX approval is authorized.

Revision 1.13 verification (2026-09-17): full/dedicated typechecks passed; 160 targeted tests (1157 assertions) passed; 70 desktop/touch browser tests passed in 2.2 minutes. The revised gallery ran last and passed (43.5 seconds), producing 74 screenshots including actual localhost and tailnet SPA/dashboard completion, explicit Open, phone stopped/running deletion, stop failure/dirty blockers and retained-registration retry. Relevant screenshots were inspected. The live demo and gallery memory snapshot were restarted with their existing exact HTTPS origins; both local/HTTPS endpoints and fresh gallery images returned 200, wrong-Origin demo mutation 403, gallery POST 405 and unknown path 404. Demo was reset to five initial rows. See `tests/e2e/WORKTREE-DEMO.md` for evidence/URLs and limitations. Task 2.2 remains unchecked; real Git/backend/PTY/provider/native behavior is not verified or authorized.

- [x] 1.12 Implement the approved no-Details simplification, parent-only Configure, retained dashboard actions and owned-only guarded deletion with branches always preserved. Implement the editable branch combobox and adjacent explicit mock fetch with selection/query preservation and disappeared-ref invalidation. Reverify context-relevant recovery and visible demo controls, targeted tests, full/dedicated typechecks, desktop/touch browser coverage and inspected screenshots; refresh the existing secured demo and review packet. Task 2.2 remains open.

Revision 1.12 verification: full/dedicated typechecks passed; 158 targeted tests (1078 assertions) and 60 desktop/touch browser tests passed. OpenSpec validation and diff checks passed. Inspected selector/dashboard and preserved/disappeared combobox screenshots. The secured loopback demo was restarted with the existing exact HTTPS origin; local/HTTPS SPA returned 200, wrong-Origin mutation 403, and HTTPS browser smoke confirmed five initial rows, no lifecycle mutations and no page errors. Review packet: `tests/e2e/WORKTREE-DEMO.md`. Earlier verification below is historical; this revision supersedes Details-based navigation and optional branch deletion. Task 2.2 is still unchecked.

- [x] 1.11 Add muted immutable branch-origin labels to nested selector and original dashboard rows without changing compact creation UI. Record actual simulated parent HEAD for new branches, selected remote ref for newly created tracking branches, and unknown for existing-local/external branches absent explicit recorded history; keep upstream/live policy distinct. Verify both row surfaces, all provenance cases and immutability after parent ref/configuration changes; run full/dedicated typechecks, targeted tests and desktop/touch E2E screenshots, refresh the existing secured loopback demo, and present evidence without completing 2.2.

Provenance revision verification: full/dedicated typechecks passed, 154 targeted tests (1041 assertions) and 56 desktop/touch browser tests passed. Fresh provenance screenshots were inspected; selector overflow was corrected and covered by a bounds assertion. The existing loopback demo was refreshed with its exact configured HTTPS origin and smoke-tested on both real row surfaces without lifecycle mutations (five initial rows, no page errors); local/HTTPS SPA 200 and wrong-Origin mutation 403. Evidence paths and limitations are in `tests/e2e/WORKTREE-DEMO.md`. Task 2.2 remains unchecked; no real integration is authorized.

The earlier grouped-parent iteration is historical evidence. The explicitly requested compact creation revision is verified under 1.10: full/dedicated typechecks, 151 targeted tests (1027 assertions), 54 desktop/touch browser tests, OpenSpec validation and diff checks passed. Fresh screenshots were inspected; the secured loopback demo and HTTPS review path were refreshed and smoke-tested. Review instructions are in `tests/e2e/WORKTREE-DEMO.md`. This supersedes the earlier base/mode/readout creation forms in 1.3/1.4, not the safety contract or approval gate. None establishes approval under 2.2.

- [x] 1.10 Preserve original dashboard rows/actions/status/style, nesting, branch labels and parent forks; remove generic Details navigation. Switcher contains switching and parent forks only. Parent Configure, unregister and owned-only guarded Delete remain context-relevant; visible test controls expose inventory/discovery/rename safety. Verify compact new-branch form and editable branch combobox with adjacent explicit Fetch, full ref names, keyboard/touch, cancellation, stale-selection prevention and recovery. Run typechecks, regressions and desktop/touch screenshots; refresh the secured demo, not UX approval.

- [x] 1.9 Revise and reverify the mock: explicit repository/main identity grouping and parent-only fork actions in the actual picker and real Hub dashboard, two parent repositories, exact branch child names, fixed sibling destinations with safe deterministic collisions, parent-only live credential/shared-configuration inheritance without inheriting checkout runtime or personal/device state, unchanged external paths/ownership, preserved safety/recovery/navigation flows. Update affected desktop/touch/unit regressions, scenario matrix and screenshots, run full and dedicated typechecks, restart the secured review endpoint and present refreshed evidence. This supersedes earlier destination/name/child-assignment forms, not the approval gate.

- [x] 1.1 Revise the loopback-only host and dedicated Playwright fixture under `tests/e2e/` to serve the actual `src/index.html`/`src/app.ts` frontend and bundled assets at separate `/s/<workspace-id>/` URLs with allowlisted in-memory document/tree/render, terminal, chat, workspace and live adapters; reuse isolated serving/fake-service patterns without side-effectful server startup. Verify base-path requests and a request ledger show no real Git, watchers, PTYs, shells, workspace children, providers, remote requests, credential stores, authentication secrets or persistent Hub/repository writes, and unknown routes fail closed without production or proxy fallback.
- [x] 1.2 Retain the reusable synthetic scenario/state foundation: visible demo controls, controllable latency and deterministic in-memory reset under `tests/`, with synthetic workspace/credential data; verification is limited to the existing fixture reset after simulated create/start/delete and absence of required or retained authentication secrets. Integration with the actual frontend and complete per-workspace/browser-state reset remain covered by reopened tasks 1.1, 1.7 and 1.8; this checkbox does not establish real-frontend completion or UX acceptance.
- [x] 1.3 Extend the existing `src/shell/hub-nav.ts` picker for creation/switching and passive inventory updates, preserving identity/activity affordances. Group children by explicit main/repository identity in picker and dashboard, with parent-only forks and exact branch names. Implement new branch from source HEAD, existing local and remote tracking modes, fixed sibling destinations, live parent policy and stopped results, without child path/name/credential forms; verify both parents and all modes without changing source context. Visible test controls expose loading/empty inventory scenarios.
- [x] 1.4 Integrate mock branch/path conflicts and direct existing-checkout Open/Start/registration actions, plus adjacent explicit fetch pending/success/auth/network/disappearance. Verify exact field value, edit invalidation, preserved valid query/choice and no stale hidden choice, force, substitution, duplicate submit or real remote/credential access.
- [x] 1.5 Integrate retained-checkout registration failure, retry-registration, start failure, external discovery and missing/replaced-path states into the actual workspace frontend; verify retry preserves simulated identity without duplicate checkouts, unregistered external Open requires explicit configuration, and discovery/live invalidation/reconnect never changes the active workspace, document/preview, terminal or conversation or silently recreates unavailable paths.
- [x] 1.6 Integrate guarded mock delete/unregister/rename safety without Details navigation, preserving branches unconditionally and removing branch-deletion UI. Verify clean success, dirty/untracked/ignored/locked/in-use/nested blockers, stop failure, exact path/branch/stop confirmation, unknown/external restrictions and visible context-relevant recovery.
- [x] 1.7 Implement independent fixture contexts keyed by workspace ID for files/selected document, preview, terminal sessions/output/selection and chat inventories/transcripts/selected conversation, including in-memory chat restoration/attachment callbacks. Verify picker create → stopped → explicit Start/Open reaches the actual frontend at a separate stable `/s/<workspace-id>/` URL without child startup; picker return and browser back/forward restore each workspace's own selections, opening indicators clear, no conversation is copied/migrated, and delayed responses/live events cannot overwrite another workspace. Verify reset clears all fixture contexts, pending events and scenario-owned navigation/browser restoration state so prior scenarios cannot reappear.
- [x] 1.8 Run desktop and narrow-touch mock E2E coverage in the actual frontend for the full section 1 matrix, including keyboard/focus/error announcements, light/dark, titlebar inset, reset, discovery/reconnect, slow operations and cross-workspace navigation races; capture real picker, real grouped dashboard and distinct files/preview/terminal/chat screenshots and round-trip behavior reports via `tests/e2e/evidence.ts` in Playwright output. Verify full and dedicated typechecks, evidence discipline and no-capability dashboard regressions; label simulated behavior and browser emulation separately from untested real runtime/native behavior.

## 2. Explicit user UX approval gate — STOP here

- [x] 2.1 Present fresh runnable real-frontend prototype instructions, the scenario matrix and desktop/touch grouped picker/dashboard, parent-policy and context-restoration evidence for user review, clearly separating simulation from untested real Git/backend/PTY/provider and native behavior; verify the review packet covers all section 1 outcomes and records remaining UX issues. Prior standalone Hub demo evidence and completion claims do not satisfy this review or the approval gate.
- [ ] 2.2 Obtain and record explicit user UX approval and resolve required prototype revisions, rerunning affected tests; verify the actual user decision is recorded before marking this task complete. Do not infer approval from planning completion or green tests. All sections below remain blocked until this gate is satisfied.

## 3. Post-approval service foundations and safety

- [ ] 3.1 Define the shared worktree operation DTOs, bounded phases, ownership/identity model and sanitized error contract, following the approved UI; verify contract fixtures cover all prototype states and existing public API publication checks accept the additions.
- [ ] 3.2 Add Git capability/ref/inventory probes and canonical common-directory identity with repository serialization plus existing path reservations; verify temporary-repository tests cover main/linked sources, unusual paths, option-like inputs, symlinks, branch collisions and concurrent operations, and document unsupported Git/submodule cases.
- [ ] 3.3 Add durable operation intent/provenance and recovery coordination with onboarding registration/assignments; verify injected persistence failures, restart at each creation boundary, idempotent retry, forget/re-register and replaced-path cases never confer false ownership or delete uncertain content.
- [ ] 3.4 Guard generic folder rename against linked/main/common-directory and ancestor dependencies, including unregistered worktrees; verify unsafe/inconclusive moves are rejected even with stop authorization while safe unrelated rename and ordinary/main display-name edits work. Child names follow their local branch; branch rename is out of scope.

## 4. Post-approval creation and authenticated operations

- [ ] 4.1 Implement bounded non-force Git creation for new branch from source HEAD, existing local and remote tracking modes; verify real temporary-repository tests assert exact branch/base/upstream, branch-in-use open hints, destination preservation and dirty-source independence.
- [ ] 4.2 Implement explicit parent-policy credential-aware remote fetch and freshness reporting without ambient/unselected credential fallback; verify parent selected/none/locked/disabled/auth failure cases and sanitized output using clean tool-environment credential tests.
- [ ] 4.3 Connect verified checkouts to atomic onboarding with an explicit main/repository relationship, live parent credential/shared-configuration inheritance and optional start; verify stopped default, updates propagating without child overrides or personal/runtime state copying, retained checkout on registration failure, same-checkout retry and preserved stopped child on start failure.
- [ ] 4.4 Expose authenticated Hub operations/progress/recovery to the UI, scoped to authorized callers; verify unauthenticated, cross-origin cookie, wrong-user operation access, timeout/cancellation and partial-resource outcomes, including bounded subprocess cleanup.

## 5. Post-approval discovery and guarded removal

- [ ] 5.1 Implement Git-based reconciliation on open, relevant activity, manual refresh and bounded periodic cadence; verify deterministic timer/activity tests coalesce refreshes, preserve external provenance and report missing/replaced paths without switching, auto-registration or recreation.
- [ ] 5.2 Add the `worktrees` invalidation topic to broker/client and public contracts; verify immediate notification after UI/CLI-equivalent operations, reconnect recovery, authorization, one live connection per visible page and unchanged content-free activity payloads.
- [ ] 5.3 Implement deletion preflight for ownership, identity, tracked/untracked/ignored data, locks, nested dependencies and known activity; verify each blocker retains checkout/registration and main/external/unknown trees cannot be deleted.
- [ ] 5.4 Implement confirm-stop-fence-recheck-non-force-remove-unregister with recovery journaling; verify failed stop/remove, in-flight/concurrent start, cleanup persistence failure and path reuse after removal do not lose files or accidentally delete a new occupant.
- [ ] 5.5 Implement provenance-preserving forget behavior and unconditional branch preservation on checkout deletion; no branch-deletion UI or operation. Verify stopped-only forget and external checkout/branch preservation.
- [ ] 5.6 Connect the approved actual frontend's existing workspace picker and secondary Hub entry points to the real Hub operation contract while retaining the isolated real-frontend mock host; verify the same state matrix against temporary repositories, independent workspace context restoration, and existing dashboard/create/clone/switcher regression tests.

## 6. Post-approval CLI and provider skills

- [ ] 6.1 Finalize proposed CLI spelling/flags and authenticated Hub-context transport with least-privilege/revocation and secret-redaction review; verify a documented threat model and contract examples cover missing/expired/wrong Hub context without assuming an installed Uatu executable.
- [ ] 6.2 Implement CLI dispatch as a thin authenticated Hub client with structured progress/results/recovery and explicit destructive confirmation; verify transport tests prove parity with UI safety, no independent Git fallback, and no secrets in arguments/output.
- [ ] 6.3 Define opt-in Claude/OpenCode skill packaging and add thin guidance for explicit persistent workspace requests; verify fixture-based loading/documentation tests preserve existing user/project configuration and do not replace native hooks or invoke experimental provider removal/reset.
- [ ] 6.4 Test actual available supported Claude/OpenCode binaries in separate workspaces, requesting permission before any installation; verify new/resumed conversation directory boundaries and native worktree/subagent coexistence, and record unavailable tools as untested rather than claiming compatibility.

## 7. Integrated acceptance and documentation

- [ ] 7.1 Run cross-surface temporary-repository acceptance through the existing workspace picker for files, terminal, preview, search, Git and chat checkout identity plus CLI-triggered live appearance; verify picker and browser-history round trips restore each workspace's own context without conversation migration, delayed responses/live events do not leak across workspaces, discovery leaves active selections unchanged, and external cleanup produces a truthful missing state.
- [ ] 7.2 Run broader unit/E2E/public-contract and recovery regressions, capture final desktop/touch evidence using existing evidence helpers, and perform a native macOS navigation smoke test when available; record exact results and distinguish browser emulation from native verification.
- [ ] 7.3 Document creation modes, parent-policy inheritance, exact branch names/predetermined destinations, external ownership, ignored data, safe delete versus forget, partial recovery, CLI/skill setup and rollback; verify examples against implemented commands and ensure no documentation claims worktree security isolation or guaranteed native transcript import.
