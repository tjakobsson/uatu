## 1. The dashboard row discloses inherited policy (#418)

- [x] 1.1 Add `workspaceCredentialSummary(w)` to the dashboard client in `src/hub/pages.ts`: summarise `workspacePolicyOwner(w).credentialAssignments` and append ` · inherited from ` + the owner's label when the owner is not the row itself. Use it for both the running and the stopped row `detail`. Verify `bun test src/hub/pages.test.ts`.
- [x] 1.2 Route the fork dialog's source and the credential card's `Restart required` notice through the same `workspacePolicyOwner` lookup in `src/hub/pages.ts`, so the notice matches an assignment naming the parent against the running child the server flagged with `credentialRestartRequired`. Verify with the `credentialCardHarness` test "credential card raises the restart notice on a running row whose policy owner the assignment names" in `src/hub/pages.test.ts`, which renders the real `credentialCard` and asserts the `.restart-required` element appears for the flagged child and is absent when the flagged row has another policy owner or no flag.
- [x] 1.3 Make `workspacePolicyOwner` fall back to the row itself when the named parent is absent from the fetched list (`src/hub/pages.ts`), rather than to a fabricated parent. Verify an inconsistent list renders the row's own policy instead of an invented owner.
- [x] 1.4 Extend the dashboard test harness in `src/hub/pages.test.ts` to run the real `refresh()` with the summary helpers, render row titles and details, and fail loudly when a slice marker moves; cover a parent with assignments, its stopped and running children, a parent with none and its child, and an unrelated workspace. Tighten the start-flow ordering assertion to the string the product now contains. Verify `bun test src/hub/pages.test.ts` (42 pass).

## 2. The credential API refuses assignments that name a linked worktree

- [x] 2.1 Add `policyWorkspaceId(workspaceId)` to `WorkspaceRegistry` (`src/hub/registry.ts`) as the one answer to "whose policy governs this workspace?", bind it once in `runHub` (`src/hub/main.ts`), and pass it to both the stored credential context resolver and `CredentialApiServices`, so the resolver and the API resolve policy identically. Verify `bun test src/hub/registry.test.ts src/hub/credential-api.integration.test.ts`.
- [x] 2.2 Add `assignmentTarget` / `assertAssignmentTarget` to `src/hub/credential-api.ts` and use them in `assign`, `assignWorkspace` and `unassign`: an id whose policy owner differs throws `CredentialApiError(409, "credentials are managed on the parent workspace <parentId>; linked worktrees inherit them")`. Verify all three routes answer 409 for a child and 200 for the parent.
- [x] 2.3 Add `CredentialApiError` and make `credentialApiError` honour its status, and move the `unknown credential` / `unknown workspace` 404 classification ahead of the 409 message patterns (`src/hub/credential-api.ts`). Verify the regression case `credentialApiError(new Error("unknown workspace: locked-repo"))` → 404 in `src/hub/credential-api.integration.test.ts`.
- [x] 2.4 Call `credentialApi.assertAssignmentTarget(workspaceId)` in the unassign route inside the lifecycle queue and before `stopWhileLifecycleQueueHeld` (`src/hub/server.ts`), so a refused request never stops the workspace. Verify the `stop: true` refusal leaves the child running with no recorded backend stop.
- [x] 2.5 Make the same assertion cover an unassignment sent WITHOUT `stop`: fold both paths into one `sessions.runExclusive` body in `src/hub/server.ts` so the target check runs after the onboarding fence and before the running-session conflict, and a running child is told which workspace owns the credentials rather than to stop a session that could never hold the assignment. Verify the no-`stop` refusal answers 409 with the parent-naming message and leaves the child running.
- [x] 2.6 Cover the refusals end to end in `src/hub/credential-api.integration.test.ts`: `/assign`, `/credential-assignments` and `/unassign` against a registered linked worktree return 409 with the parent-naming message and match the published contract; the parent is still assignable and unassignable; `stop: true` on the child leaves it running and the assignment intact. Verify `bun test src/hub/credential-api.integration.test.ts` (36 pass).
- [x] 2.7 Supply the `policyWorkspaceId` service in the remaining `createHubFetchHandler` and resolver fixtures (`src/hub/worktree-api.integration.test.ts`, `src/hub/worktree-lifecycle.integration.test.ts`, `src/hub/worktree-onboarding.test.ts`) by delegating to `registry.policyWorkspaceId`, so no fixture restates the parent lookup. Verify `bun test src/hub/worktree-api.integration.test.ts src/hub/worktree-lifecycle.integration.test.ts src/hub/worktree-onboarding.test.ts` in a clean tool environment.

## 3. Verification and closing

- [x] 3.1 Run `bun test` and confirm the unit suite, with `src/hub/pages.test.ts` (43 pass), `src/hub/credential-api.integration.test.ts` (36 pass) and `src/hub/registry.test.ts` green. _Result: 3967 pass, 11 skip, 4 fail across 238 files. The 4 are `src/hub/credential-context.test.ts` cases that read ambient Git config and fail in this Hub-managed workspace, whose projected Git/SSH wrappers they discover (see CLAUDE.md); they are untouched by this change and need a clean tool environment._
- [x] 3.2 Confirm the client and the API now agree on who owns policy: the Settings assignment form in `src/hub/pages.ts` already omits linked worktrees from its workspace selector, so the 409 closes a gap the UI never offered rather than refusing an action the UI still presents. Verify by reading `workspaceAssignmentForm`'s `dashboardWorkspaces.filter(item => !item.parentId)`.
- [x] 3.3 Run `openspec validate fix-worktree-inherited-credentials --strict` and confirm the delta matches the implemented behavior.
- [x] 3.4 Determine the stable impact and record the release-note decision in the proposal's Impact: linked worktrees shipped in #395, after the latest stable tag `v0.7.0`, so no released build shows this defect. Verify with `git describe --tags --abbrev=0` against `v0.7.0` and the merge order of #395; the PR keeps its `fix(hub)` title and its body must carry the override `chore(hub): stabilize the unreleased feature before release` before squash merge.
- [x] 3.5 Cover the three behaviors on a real Hub in `tests/e2e/worktree-credentials.e2e.ts` (the e2e hub gains an opt-in `hubCredentials` worker option in `tests/e2e/hub-fixtures.ts` / `tests/e2e/hub-server.ts` that mounts the token credential API with a parent-reading resolver, so `credentialRestartRequired` is the server's own flag): the dashboard row of a worktree forked from the dashboard reads `🔑 Auth: <name> · inherited from <parent>` on desktop and touch while the parent's row carries no `inherited`; `/assign`, `/credential-assignments` and `/unassign` (with and without `stop`) naming the child answer 409 with the parent-naming message and the row still reads the inherited summary; a running child whose stopped parent's assignment is replaced is flagged and the Settings card naming the parent shows `Restart required`. Verify `bunx playwright test tests/e2e/worktree-credentials.e2e.ts` (4 pass; also 8 pass serial with `--workers=1 --repeat-each=2`), `bun test tests/evidence-discipline.test.ts`, and the captures assembled with `UATU_E2E_SCREENSHOTS_DIR=<this change's screenshots folder>` (`worktree-credentials-{desktop,touch}-dashboard{,-dark}.png`, `worktree-credentials-restart-required.png`, `worktree-credentials-refusal.json`).
