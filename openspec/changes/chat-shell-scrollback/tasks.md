## 1. Reproduce and stabilize latest following

- [x] 1.1 Build a deterministic streaming fixture and test-only frame trace of scroll position, extent, viewport height, follow intent, and correction source. Exercise passive following, latest activation, upward reader scrolling, intrinsic-size changes, resize, and completion in parent and child timelines on Chromium and WebKit. Use the confirmed competing-correction and clamp-intent defects as the approved reproduction baseline; keep the reported alternating oscillation explicitly unconfirmed and distinguish legitimate shrink/clamping from jitter.
- [x] 1.2 Coordinate automatic scrolling from render, scroll observation, item resize, viewport, and activity toggle paths through one owner per scroll container, using the reproduction evidence; verify at most one automatic position correction per frame and no alternating up/down movement during stable-extent append-only following.
- [x] 1.3 Make latest actions and continuous following use immediate coordinated positioning, distinguish delayed programmatic/clamped echoes from reader input, and cancel pending following on upward input; verify latest settles during appends without oscillation, small upward input pauses following, and existing history, resize, navigation, and anchor tests still pass.

## 2. Incremental terminal text

- [x] 2.1 Add a stateful feed API sharing `src/chat/ansi.ts` interpretation rules, retaining incomplete controls, styles, Unicode cell behavior, and mutable progress lines; verify incremental output matches one-shot output across split escape, Unicode, overwrite, and hostile-content fixtures in colocated ANSI tests.
- [x] 2.2 Add cumulative-output append detection and authoritative replacement/reset handling for shell output; verify append processing touches only new input plus mutable parser state, and shortened or corrected snapshots produce the authoritative rendered result.

## 3. Stable shell scrollback

- [x] 3.1 Add a conversation/item-owned shell-output controller with a stable bounded-height viewport and incremental line updates; verify unchanged line nodes and selected text survive appends and all supplied lines remain available without a preview disclosure or omitted-tail notice.
- [x] 3.2 Wire normalized command items and Bash tool details into persistent renderer patching, including metadata, errors, completed-only output, lazy materialization, grouping, and reader-open/closed rules; verify renderer tests cover both shapes, completion/failure, regrouping, and unchanged non-shell previews.
- [x] 3.3 Implement independent bottom-follow, upward-scroll pause, latest-output indication/resumption, and horizontal/vertical reading anchors through the coordinated scroll owner; verify controller tests cover small upward scrolls, programmatic scroll echoes, content replacement, layout clamping without accidental follow resumption, and completed-only initialization.
- [x] 3.4 Integrate both parent and child render scheduling and controller disposal; verify hidden Chat and hidden pages defer output painting, retained view state remains conversation-scoped, and item removal, renderer reset, and navigation release listeners and stale ownership.

## 4. Inline sizing and floating output

- [x] 4.1 Add pointer/touch and keyboard inline height controls with accessible size/bounds, visible focus, container-size clamping, and coordinated anchor handling; verify resizing preserves output and outer reading positions, marks the row and containing group reader-opened, and keeps controls reachable in small layouts.
- [x] 4.2 Add the app-level nonmodal floating output host outside Chat width/overflow constraints, Pop out actions for parent and child items, and a stable inline placeholder; verify only one item owns the window, output can exceed both Chat text and panel widths, terminal styling is retained, and uncovered app content remains interactive.
- [x] 4.3 Add pointer and keyboard movement and two-axis resize controls with work-area bounds retained by conversation/item identity; verify current values/bounds and visible focus are accessible, controls cannot be dragged offscreen, output position/follow state survive manipulation, and inline height remains separate from floating geometry. Verify resizing item B does not overwrite item A's bounds, including after retained-view navigation and a later explicit Pop out.
- [x] 4.4 Add Maximize, Restore size, and touch full-area presentation; verify maximize fills the app work area, restore recovers the saved floating rectangle within bounds, desktop/touch changes preserve geometry, and Return to chat remains reachable while covered work-area controls cannot receive focus.
- [x] 4.5 Implement Return to chat, optional close-button equivalence, Escape priority, focus/anchor restoration, and lifecycle cleanup; verify Escape returns directly inline even when maximized without closing the child, explicit conversation/owning-child navigation releases the window, hidden Chat hides it, and return preserves both output state and any deliberate outer scrolling performed while it was open.
- [x] 4.6 Add command/conversation identity and provider outcome to the window header, with completion time only when supplied; verify completion/failure/cancellation and later commands leave the selected log open without retargeting it, completed-only output gets truthful metadata, and disconnection does not masquerade as completion.
- [x] 4.7 Connect floating, maximized, touch, and lazily materialized output to existing find and copy/selection behavior; verify searches reach early and late output without duplicate searchable content, and selection in unchanged lines survives streaming.

## 5. Integration verification

- [x] 5.1 Extend `tests/e2e/chat-shell-output.e2e.ts` with the full running-output flow: scroll upward, append, resize inline, pop out, move and resize beyond Chat width, maximize, append and complete, restore size, then Escape return. Verify both shell item shapes and child transcript behavior, plus completed-only, failed/cancelled, ANSI/progress rewrite, horizontal overflow, and non-shell preview regression cases.
- [x] 5.2 Verify desktop and touch layouts, light/dark terminal styling, pointer and keyboard controls, small work-area bounds, and software-keyboard/container resizing through browser tests and screenshots; confirm floating output exceeds Chat constraints, uncovered app controls stay usable, and maximized/touch output always offers a reachable return path.
- [x] 5.3 Re-run the frame-level follow fixture after integrating scrollback and the floating window on Chromium and WebKit. Verify passive following, latest activation during appends, upward interruption, resize/clamping, pop-out/return, and completion in both outer timelines and shell viewports without competing automatic corrections; retain the confirmed competing-correction and clamp-intent cases as regression tests without claiming reproduction of the reported oscillation.
- [x] 5.4 Add a repeatable long-output responsiveness case using existing chat responsiveness fixtures. Record input size, update count, and parse/DOM work; verify ordinary appends do not reparse or replace the whole log, hidden Chat does not paint it, earliest supplied output stays inspectable, and navigation/move/resize remain responsive.
- [x] 5.5 Add a retained-view browser case that hides Chat with output popped out, appends output and completes the command while hidden, then returns; verify no hidden painting, restoration of the same window and owning command with current output/final status, and retained geometry and reading state.
- [x] 5.6 Run the affected colocated unit tests and focused shell-output/responsiveness Playwright suites using the repository's configured commands; record results and resolve failures before marking the change complete.
