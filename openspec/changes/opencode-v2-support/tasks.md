## 1. Readiness decides the generation

- [x] 1.1 In `src/chat/opencode/opencode-service.ts`, make each probe cycle of `waitUntilReady` request `GET /api/info` then `GET /global/health` (both with the basic-auth header, each individually bounded), accept only a well-formed body (`/api/info` → string `version`; `/global/health` → `healthy === true`), and resolve `{ generation: 1 | 2, version }` with the version normalized to bare semver (strip a leading `opencode v`). Keep `ProbeProgress.answered` flipping on any HTTP response. Verify with `src/chat/opencode/opencode-service.test.ts` cases: health healthy → generation 1; info `{version:"2.0.13"}` + health `200 text/html` → generation 2 and version `2.0.13`; info 404 + health healthy → generation 1; info 401 + health HTML for the whole health budget → unavailable on the short budget.
- [x] 1.2 Record the last outcome per path (`{ health, info }`) in `ProbeProgress`, and make `describeProbe` name both in the "answered but never became healthy" message (e.g. `health: 200 with a non-health body; info: 401`). Verify with a service test asserting the failure message contains both paths' outcomes, and that the existing single-outcome messages for the bind phase are unchanged.
- [x] 1.3 Add `generation` to `OpenCodeConnection`; set `OPENCODE_PASSWORD` alongside `OPENCODE_SERVER_PASSWORD` in `buildOpenCodeEnvironment`; add the new name to the diagnostics redaction. Verify with service tests: the spawned env carries both variables with the same secret, and a capture containing the secret is redacted under both names.

## 2. Two stacks under one folder

- [x] 2.1 `git mv` `src/chat/opencode/sdk-v2-provider.ts` → `src/chat/opencode/v1/provider.ts`, `normalization.ts` → `v1/normalization.ts`, and their tests alongside; rename the export to `createOpenCodeV1Provider`; update imports in `src/chat/service.ts` and tests; update `CLAUDE.md`'s folder map and `ARCHITECTURE.md`'s chat section for `v1/` and `v2/`. Verify `bun test src/chat` passes with no behavior change and `git log --follow src/chat/opencode/v1/provider.ts` shows the history.
- [x] 2.2 Add `@opencode/client` pinned at `2.0.13` to `package.json` (beside `@opencode-ai/sdk`), install, and verify `bun run check:licenses` passes with `@opencode/client`, `@opencode/schema`, `@opencode/protocol` listed as MIT.

## 3. One normalization core, two mappers

- [x] 3.1 In `src/chat/opencode/normalization.ts` (shared), key the `session.next.*` handlers by their canonical 2.x names and have the 1.x mapper (`v1/normalization.ts`) rename `session.next.X` → `session.X` before dispatch, keeping its `message.*` dedup and `question.*` handling intact. Verify `src/chat/opencode/v1/normalization.test.ts` passes unchanged — its fixtures are the contract.
- [x] 3.2 Write `src/chat/opencode/v2/normalization.ts`: pass session events through; map `session.execution.{started,succeeded,failed,interrupted}` → turn lifecycle, `session.usage.updated` → context usage, `form.{created,replied,cancelled}` → structured question raised/answered/rejected (one question per field per design), `session.message.content.updated` → restatement deduped by message id + ordinal against streamed text, `session.{agent,model}.selected` → configuration update, and drop events whose `location.directory` is not the workspace's. Verify with `src/chat/opencode/v2/normalization.test.ts` using fixtures captured from a real 2.x `/api/event` stream (a prompt with a tool call, a form, a permission, an interrupt, a compaction): each produces the same normalized events the 1.x fixtures produce for the same activity, a foreign-directory event yields nothing, and an unknown type increments the per-type discard counter.

## 4. The 2.x provider

- [x] 4.1 Create `src/chat/opencode/v2/provider.ts` with `createOpenCodeV2Provider({ endpoint, password, directory })` building `OpenCode.make({ baseUrl, headers: { authorization } })`; implement `describe()` (capabilities per design), `listCommands`, `listModels` (variants, image input from `capabilities.input.image`), `listModes` (`agent.list`), `newConversationConfiguration` (`model.default`), all scoped by `location.directory`. Verify with `src/chat/opencode/v2/provider.test.ts` against a fake fetch that asserts request paths under `/api/…`, the auth header, and the directory in each input.
- [x] 4.2 Implement sessions: `listSessions` (`session.list` with `directory`, filtering `SessionInfo.location.directory`), `createSession` (`location.directory`, agent/model from configuration), `getSession` (foreign directory → null), `renameSession` (`session.update`), `getConversationConfiguration`, `switchModel`. Verify with provider tests: a foreign-directory session is refused, rename sends `title`, and `ProviderSession` fields (`directory`, `parentId`, timestamps) map from `SessionInfo`.
- [x] 4.3 Implement `listMessages` over `session.message.list` (`order: "desc"`, `limit`, `cursor`) mapping every `SessionMessageInfo` variant to conversation items and building `accounting` from the assistant record; keep `nextCursor` opaque. Verify with provider tests on a captured two-page history: items are in conversation order, tool states map to the same item shapes the 1.x provider yields, and the second page continues from the returned cursor.
- [x] 4.4 Implement `prompt` (`session.prompt` with `delivery: "queue"`, model/agent/variant, `files` as `file://` URIs), `command` (`session.command`), `interrupt` (`session.interrupt`), and `events(signal)` over `client.event.subscribe({ signal })` fed to the 2.x normalizer. Verify with provider tests: the prompt body carries the attachment URIs and the queue delivery, interrupt hits `/api/session/:id/interrupt`, and aborting the signal ends the event iterator.
- [x] 4.5 Implement `listPermissions`/`replyPermission` over `permission.request.list/reply` (patterns from `save`, subagent attribution from `source`), and `listQuestions`/`replyQuestion`/`rejectQuestion` over `session.form.list/reply/cancel` with the field→question mapping and ordered answers folded back to `{ [key]: value }`. Verify with provider tests: one reply per request, a multiselect answer is an array, a custom string answer is sent as the field's value, number/boolean answers are validated before reply, and a cancelled form records rejection.
- [x] 4.6 Implement `getReversibleHistoryState`, `undo`, `redo`, `revert`, `restore` over `session.revert.stage/commit/clear` and `SessionInfo.revert`. Verify with provider tests mirroring the 1.x reversible-history cases (staged boundary, walk back by user turn, redo clears, commit on replacement prompt).

## 5. Wiring

- [x] 5.1 In `src/chat/service.ts`, replace the single `createProvider` default with a per-generation map `{ 1: createOpenCodeV1Provider, 2: createOpenCodeV2Provider }` keyed by `connection.generation`; keep the test seam as a map. Verify with a service test that a ready connection of generation 2 builds the 2.x provider and generation 1 the 1.x one, and that a restart whose probe decides differently swaps the provider.

## 6. Real OpenCode, both generations

- [x] 6.1 Parameterize `src/chat/opencode/real-opencode.integration.test.ts` by `UATU_REAL_OPENCODE_V1` / `UATU_REAL_OPENCODE_V2` (absolute binary paths): for each that is set, build a temp `bin/` with an `opencode` symlink, prepend it to the child `PATH`, run the shared assertions plus a `version` major assertion; the 2.x run sets `XDG_DATA_HOME`/`XDG_STATE_HOME`/`XDG_CACHE_HOME` to a temp root. With neither set, the existing `PATH` run is unchanged. Verify by running `UATU_REAL_OPENCODE=1 UATU_REAL_OPENCODE_V1=$(which opencode) UATU_REAL_OPENCODE_V2=<2.x binary> bun test real-opencode` locally and confirming both generations pass; prune any capability the 2.x run cannot demonstrate from the 2.x `describe()`; settle the design's two open questions (per-message accounting carrier, `PermissionReplyInput` vocabulary) and record the answers in `design.md`.
- [x] 6.2 Add the side-by-side developer note to `ARCHITECTURE.md`: the 2.x zip the `anomalyco/tap/opencode-v2` formula points at, the wrapper script exporting the `XDG_*` roots, `opencode auth login` inside it, and why the isolation matters (2.x migrates a shared database). Verify the note's commands work from a clean shell on macOS.
- [x] 6.3 Run the dev hub with the 2.x wrapper first on `PATH`, hold a conversation that streams a tool call, raises a permission with saved patterns, raises a form, and is undone once; save shots of the streaming turn, the form, and the persistent-approval confirmation under `openspec/changes/opencode-v2-support/screenshots/`. Verify the timeline, cost chip, context indicator, and undo dock read as they do on 1.x.
