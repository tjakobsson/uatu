## 1. Reproduction and integration prerequisites

- [ ] 1.1 Reproduce the installed-PWA desktop-mode bug on an 11-inch iPad Pro with Magic Keyboard; record OS/browser version, safe-area values, visual viewport height/offset/scale, page scroll, host marker, blur layers, and header/composer rectangles before and after focus. Verify the evidence identifies which controls leave the usable area and whether the blur is native or application-rendered.
- [x] 1.2 Verify a maintained Web Push dependency against Bun and the compiled distribution, pin it, and add the sender interface/test double. Completion requires a successful build and `bun run check:licenses`, plus a transport smoke test exercising VAPID and encrypted payload construction without production credentials.

## 2. Shared agent events and child feed

- [x] 2.1 Define the provider-independent notification occurrence and interaction-resolution types with stable request/turn identity. Verify unit cases distinguish simultaneous conversations, repeated terminal frames, and separate pending questions.
- [x] 2.2 Establish provider-correlated turn identity and final outcomes for OpenCode native and compatibility events, then emit live notification occurrences after workspace classification. Verify successful top-level turns, questions, and permissions without a selected conversation; late completion of turn A after turn B starts and stream reconnect must not misidentify B. History, unqualified idle/status frames, intermediate model steps, failures, cancellation, tools, and child completion must not produce successful-turn alerts.
- [x] 2.3 Emit Claude's correlated notification lifecycle from SDK execution/result handling after cancellation and background-work outcomes are known. Verify questions/permissions and successful turns, live-versus-transcript distinction, duplicate results with queued prompts, unprompted follow-up turns, and that completed-then-background emits no successful-turn alert.
- [x] 2.4 Expose the combined internal child notification feed through `buildRoutes(deps)` with bounded replay and an atomic pending-snapshot/live handoff. Verify cursor replay/gaps, original request timestamps, subscription cancellation, and that subscribing starts neither a workspace nor an unused runtime but observes an agent started later.

## 3. Hub enrollment and sender identity

- [x] 3.1 Persist the VAPID keypair, notification contact configuration, and versioned enrollment records in the private hub state root. Verify restart preserves keys/preferences, invalid sender configuration is reported, and the client never receives the private key.
- [x] 3.2 Add authenticated capability, enrollment reconciliation, preference update, and removal operations. Verify hub request protections, ownership, endpoint validation, explicit workspace/category selection, concurrent same-context reconciliation, and independent device preferences in integration tests.
- [x] 3.3 Bind enrollment authorization to the hub user/session and current workspace access. Verify logout, expiration, revocation, access loss, disable, and account switching prevent future sends and cannot silently transfer an endpoint to another user.
- [x] 3.4 Publish the new Hub operations and schemas and explicitly exclude the internal child feed in route coverage. Verify `bun run api:validate` and `bun run test:api`, including documented success/error responses and any required API revision/changelog update.

## 4. Background observation and delivery

- [x] 4.1 Add the hub notification coordinator with one feed per interested running workspace, source cursors, start/stop recovery, and release on final enrollment removal. Verify multiple devices share one upstream, no page connection is required, and existing hidden-page/broker release tests still pass.
- [x] 4.2 Implement atomic event/enrollment journal insertion with upstream cursor persistence, bounded retention, and terminal delivery records. Verify restart/replay deduplication, no history alerts on fresh enrollment, and pending-request reconciliation after a replay gap.
- [x] 4.3 Implement sends with generic notification copy, a stable notification identity, current authorization/preferences checks, known-resolution cancellation, and a five-minute source-event expiry. Verify a fake transport receives only eligible event/device pairs and no question/tool/transcript bodies.
- [x] 4.4 Add bounded retry, remaining-lifetime TTL, rate-limit handling, and permanent-endpoint removal. Verify temporary errors recover without a new logical event, accepted events are not resubmitted, expired events stop, and configuration failures do not delete valid enrollments or retry tightly.
- [x] 4.5 Exercise full child-to-hub delivery with all pages closed, two devices, and both agent integrations. Verify the fake push transport receives one logical delivery per eligible event/device while observation and queued work recover from hub/feed interruption.

## 5. Push worker, enrollment UI, and navigation

- [x] 5.1 Add explicit hub boot context and shared hub URL construction for notification routes and the worker, retaining session-prefixed URLs for conversation destinations. Verify the URL-discipline suite and generic-base-path tests prevent origin-wide registration outside a hub.
- [x] 5.2 Serve and bundle the hub-owned push-only worker with visible notifications, stable tags, and same-origin notification-click validation. Verify public script update access, the built-binary asset, notification payload handling, and that normal requests are not intercepted or cached.
- [x] 5.3 Revise and sequence legacy-worker cleanup and current-registration reconciliation. Verify active/waiting/installing combinations, upgraded root/session workers, current-worker survival across sibling workspaces, unrelated-worker preservation, root-scope conflicts, and recovery after mixed-version cleanup removes a registration.
- [x] 5.4 Add Notifications controls and the per-device enrollment/preferences form to the dashboard and workspace sidebar, including the collapsed sidebar. Verify initial workspace/category selections, explicit submit, permission prompts only from a user gesture, denied/unsupported/HTTP/Home Screen guidance, sender-configuration errors, and renewal after stale enrollment.
- [x] 5.5 Add agent-qualified conversation destinations and mode-aware notification opening, including validated return-through-login. Verify an existing suitable window or new window reaches the correct conversation, unrelated drafts are preserved, and missing/inaccessible targets show an unavailable state.
- [x] 5.6 Add browser integration coverage for worker lifecycle, permission/subscription reconciliation, disabled preferences, click navigation, and one OS-notification path when a page is visible. Verify the feature suite passes without requiring a real push provider; label simulated events as such in evidence.

## 6. iPad desktop viewport correction

- [x] 6.1 Add shell-owned visual viewport geometry with frame-coalesced resize/scroll handling, normal-scale behavior, and attach/detach cleanup. Verify unit coverage for small accessory-bar occlusions, nonzero viewport offsets, missing API fallback, pinch-zoom separation, and live mode switching.
- [x] 6.2 Apply safe-area-aware desktop work-area sizing and status-strip treatment using the supplied screenshots and controlled browser geometry, as authorized by the user. Verify sidebar identity/mode controls, preview actions, and conversation controls are outside the covered region, with zero-inset desktop and native macOS titlebar regression coverage. Physical confirmation remains in tasks 1.1 and 7.2.
- [x] 6.3 Coordinate chat and terminal geometry with the shell so keyboard adjustment is applied once in desktop mode and touch fullscreen sizing remains correct. Verify composer/send visibility and pane scrolling through focus/blur, dismissal, rotation, collapsed sidebar, and narrow/stacked layout transitions without losing drafts or selection.
- [x] 6.4 Add iPad-sized browser regressions for the original desktop-mode focus shift and safe-area overlap using controlled viewport measurements. Verify header/composer bounds and restored geometry rather than screenshots alone, and run related touch/chat/terminal/titlebar suites.

## 7. Device acceptance and operational documentation

- [ ] 7.1 Verify real Web Push from an HTTPS hub in a supported desktop browser and installed iPhone/iPad PWAs. Record user-gesture enrollment, questions/permissions, successful turn completion, background/locked-device delivery, and tapping through to the correct conversation; do not mark this complete from mocked browser events.
- [ ] 7.2 Repeat the reported 11-inch iPad Pro/Magic Keyboard case after the fix and record before/after evidence. Verify focus/blur, accessory-bar show/hide, software keyboard, landscape/portrait, collapsed sidebar, mode switching, and zoom with readable headers, reachable send control, preserved drafts, and no stranded gap/scroll offset.
- [x] 7.3 Document hub notification configuration, HTTPS/Home Screen prerequisites, device/workspace preferences, session-expiry behavior, sender availability, rollback, and delivery limitations. Update `ARCHITECTURE.md` for the worker, internal feed, notification persistence, and shell viewport ownership; verify documentation matches the implemented configuration and lifecycle.

## 8. Final checks and release evidence

- [x] 8.1 Run `bun run typecheck`, `bun test`, `bun run api:validate`, and `bun run test:api` after integration; verify required checks pass and credential tests use the documented clean tool environment when nested projection applies.
- [x] 8.2 Run `bun run test:e2e` and verify browser regressions pass, with screenshots/evidence written through `tests/e2e/evidence.ts` to Playwright output rather than hard-coded change paths.
- [x] 8.3 Run `bun run build`, `bun run smoke`, and `bun run check:licenses` for the finished implementation; verify the single-file binary serves the worker and the chosen push sender works in the compiled runtime.
- [x] 8.4 Run `openspec validate web-push-and-ipad-desktop --strict` and review implemented behavior against all four deltas. Verify every completed task has evidence and any blocked physical-device checks remain unchecked and reported.
- [x] 8.5 Before preparing a fix PR, determine whether the iPad bug exists in the latest stable tag and record the result. Verify the PR uses the required Release Please override for an unreleased-only correction and assembles relevant screenshots using the repository's documented PR-time evidence workflow.

## Execution notes

- After the notification implementation, the user authorized implementing the iPad fix from the supplied screenshots and browser regression tests, with final physical-device verification left to them. Simulator testing did not reproduce the original behavior; tasks 1.1 and 7.2 therefore remain unchecked.
- Task 7.1 remains pending. Browser enrollment tests simulate permission and PushManager responses; sender tests use fake transports. Neither establishes real OS push delivery to a locked iPhone/iPad.
- PR preparation is now requested. The latest stable tag is `v0.7.0`; its desktop shell still uses `100vh`, and only touch chat consumes the visual-viewport dimensions. The iPad correction therefore addresses stable-release behavior and should remain visible in release notes. Physical acceptance is assigned to the user as part of PR testing.
- PR [#400](https://github.com/tjakobsson/uatu/pull/400) contains the implementation summary, automated verification, stable-release classification, screenshot evidence, configuration instructions, and an unchecked manual-device checklist. Tasks 1.1, 7.1, and 7.2 remain open for the user's PR testing.
- Unit verification: `bun run typecheck` passed. The full `bun test` run with a clean tool PATH passed 3,431 tests, skipped 10, and failed none. The initial inherited environment exposed the documented projected Git/SSH wrappers; product credential behavior was not changed for that environment.
- API verification: `bun run api:validate` passed with the pre-existing WorkspaceConflict composition warning; `bun run test:api` passed all 182 tests.
- Browser verification: the full suite initially passed 684 tests and failed 71. Installing the required Playwright WebKit build, fixing the notification permission fixture, and rerunning all failures resolved every failure. The current notification suite passes eight tests, including enrollment renewal, unrelated-worker preservation, desktop/touch/narrow navigation, login return, and draft preservation.
- Build verification: `bun run build`, `bun run smoke`, and `bun run check:licenses` passed. The smoke checks cover the compiled worker/client assets and persisted VAPID public key. `tests/push-sender-smoke.ts` also passed as a signed, compiled Bun binary using generated test keys.
- Provider/feed verification includes `notifications.test.ts`, both provider notification lifecycle suites, adapter/provider tests using `tests/notification-pipeline.ts`, and hub notification tests. The pipeline sends each event to two devices with no page and resumes through a hub coordinator restart using persisted cursors and delivery state.
- iPad implementation verification: 39 focused unit/evidence-discipline tests and TypeScript checks passed. Eight new Chromium/WebKit geometry tests passed, with screenshot and geometry evidence produced through `tests/e2e/evidence.ts`. The 71 related chat, touch-scroll, terminal, native-titlebar, and notification browser cases passed across the initial run and focused reruns. One touch-history anchor assertion failed once; three isolated repetitions and its complete 14-test suite then passed without code changes. The rebuilt binary and its compiled-browser smoke test passed after the viewport changes.
- The iPad safe-area/viewport correction is implemented. Tasks 1.1 and 7.2 remain manual physical-device checks, not remaining implementation work. Verify the top headers before and after focusing the composer with Magic Keyboard, keyboard dismissal, rotation, and switching modes.
- During PR testing, the user supplied physical-iPad screenshots with runtime build badge `d8392fc`, before and after focusing the composer. The preview/chat headers stay in place and clear of the earlier top blur while the Magic Keyboard accessory bar appears; the composer remains above it. The pair also exposed the new Notifications button crowding the brand row. That control now has its own sidebar row, a bell in the collapsed rail, and Chromium/WebKit coverage at 280px, 320px, and 360px sidebar widths and in the touch Files tab. The remaining physical rotation, dismissal, software-keyboard, and real push-delivery checks are still pending.
- PR-time evidence was assembled with `UATU_E2E_SCREENSHOTS_DIR=openspec/changes/web-push-and-ipad-desktop/screenshots bun run test:e2e tests/e2e/ipad-desktop-viewport.e2e.ts --workers=1`: all eight cases passed. Four screenshots and two geometry reports are included with the change. They are controlled browser evidence, not physical-device confirmation.
- Strict OpenSpec validation and `git diff --check` passed. The change remains open for physical-device acceptance and PR preparation.
