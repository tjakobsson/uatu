## 1. The touch surface recovers when the page returns (#410)

- [x] 1.1 Add a `resync` handler to `ChatViewportController` in `src/chat/viewport.ts`, bound in `start()` to `document` `visibilitychange`, window `pageshow`, and window `focus`, and unbound in `stop()`. It applies immediately, again on the next animation frame, and once more after a short settle timeout, cancelling any frame or timer still outstanding. Verify `bun test src/chat/viewport.test.ts` still passes and `stop()` leaves no listener, frame, or timer behind.
- [x] 1.2 Record a correction withheld because `document.visibilityState === "hidden"` and replay it on the first `apply()` that finds the page visible, clearing the flag (`src/chat/viewport.ts`). Verify the flag is cleared after one replay, not on every later apply.
- [x] 1.3 Unit-test the recovery in `src/chat/viewport.test.ts` with a fake `visualViewport`: shrink to keyboard height, restore the height silently (no `resize` event), dispatch `visibilitychange` on `document` → `--chat-visual-height` returns to the full height and the suppressed correction is requested exactly once. Verify the test fails against the current controller.

## 2. Keyboard detection and the pinned-track budget (#354, #382)

- [x] 2.1 Change the keyboard predicate in `chatViewportMetrics` (`src/chat/viewport.ts`) to `(layoutHeight − visualHeight) > Math.max(80, tabBarInset)`, leaving `occluded` in its existing role for `tabInset`. Verify the existing `src/chat/viewport.test.ts` assertions still pass unchanged.
- [x] 2.2 Add the reported full-pan case to `src/chat/viewport.test.ts`: `chatViewportMetrics(508, 266, 844, 0)` → `keyboardVisible` is true. Verify it fails before 2.1 and passes after.
- [x] 2.3 Add `#chat-background-tasks-items` to the `max-height: 8.5rem; overflow-y: auto; overscroll-behavior: contain` cap in `src/styles.css` (~8773), beside `#chat-task-list-items` and `#chat-subagents-items`. Verify the rendered list scrolls within its cap rather than growing.
- [x] 2.4 Add `html[data-chat-keyboard] #chat-background-tasks-items` to the keyboard hide rule in `src/styles.css` (~8780). Verify the track's summary line remains while its rows are hidden with the keyboard up.

## 3. A request's answer controls stay operable (#354, #382)

- [x] 3.1 Reserve the outstanding-requests pill's clearance in `src/styles.css`, beside the existing `.chat-latest:not([hidden]) + .chat-requests-jump` rule (~6456): `.chat-transcript-area:has(> #chat-requests-jump:not([hidden])) .chat-timeline { padding-bottom: 3.5rem; scroll-padding-bottom: 3.5rem; }`. Verify a request card at the end of the conversation is fully reachable with the pill shown, and that the padding disappears when the pill is hidden.
- [x] 3.2 In the question `change` handler in `src/chat/ui.ts` (~2966), call the coordinated scroll owner's `beforeMutation(<the card's `data-chat-item-id`>)` before `syncQuestionControl(input, true)` focuses the custom editor, using the existing `preferredItemId` seam. Verify the correction that follows holds the question card rather than the topmost visible item.
- [x] 3.3 Confirm the parent and drill-down owners are both handled by 3.2 (a question card can be shown in either) and that a card without a resolvable item id falls back to today's behavior. Verify `bun test src/chat/ui.test.ts` passes.

## 4. Caret movement does not move the conversation (#354)

- [x] 4.1 Coalesce every listener in `ChatViewportController` into a single `requestAnimationFrame`-scheduled apply, cancelled in `stop()` (`src/chat/viewport.ts`). Verify N `scroll` events dispatched in one turn with a stubbed `requestAnimationFrame` produce at most one correction, as a case in `src/chat/viewport.test.ts`.
- [x] 4.2 Cache the last written height and top and skip `setProperty` when the value is unchanged, so an apply that changes nothing cannot re-enter through the controller's own `ResizeObserver` (`src/chat/viewport.ts`). Verify a repeat apply with identical metrics writes no CSS variable.
- [x] 4.3 While `html[data-chat-editing]` is set and only the pan offset changed since the last apply, write the geometry but do not call `requestCorrection()` (`src/chat/viewport.ts`). Do not introduce any CSS rule keyed on `data-chat-editing`. Verify a `src/chat/viewport.test.ts` case where `data-chat-editing` is set and only `offsetTop` changes requests zero corrections, while a height change during editing still requests one.
- [x] 4.4 Add the text-control exclusion `keyDown` already applies (`closest("input, textarea, select, [contenteditable=true]")`) to `touchStart` and `touchMove` in `src/chat/coordinated-scroll.ts` (~161-166). Verify with a case in `src/chat/coordinated-scroll.test.ts`: `touchstart` then an upward `touchmove` targeting a text input inside the scroller does not `pause()` and leaves the anchor pinned, while the same gesture on the transcript still pauses.

## 5. Browser coverage

- [x] 5.1 Extend the fake-`visualViewport` test in `tests/e2e/chat-touch.e2e.ts` (~263-315) with the background-return case: shrink to keyboard height with a `resize`, restore the height with no event, then `document.dispatchEvent(new Event("visibilitychange"))` → `--chat-visual-height` is the restored height and the composer is inside the visual viewport. Verify the case fails before task 1.
- [x] 5.2 Add a panned-keyboard case to `tests/e2e/chat-touch.e2e.ts`: viewport height 508 with `offsetTop` 266 against an 844 layout → `html` keeps `data-chat-keyboard` and the pinned tracks stay hidden.
- [x] 5.3 Add a track-budget case to `tests/e2e/chat-touch.e2e.ts`: populate and expand `#chat-task-list`, `#chat-subagents`, and `#chat-background-tasks` with the keyboard open → the composer's bottom stays within the visual viewport, as the existing case asserts for the queue and reverted lists.
- [x] 5.4 Add an answer-clearance case to `tests/e2e/chat-touch.e2e.ts` inverting the existing `document.elementFromPoint` probe (~249): with the pill shown and a question's custom-answer field open at the end of the conversation, the element painted at the field's centre is the input, and the element at the Answer control's centre is that control — not `chat-requests-jump`.
- [x] 5.5 Keep the new browser cases on `tabBarInset = 0` geometry (tab bar hidden while editing) so they do not encode the inset behavior draft PR #358 removes. Verify no new case touches `src/shell/tab-bar.ts` or the keyboard tab-bar rules at `src/styles.css` ~8020-8045.

## 6. Verification and closing

- [x] 6.1 Run `bun test` and verify a green suite, with `src/chat/viewport.test.ts`, `src/chat/coordinated-scroll.test.ts`, and `src/chat/ui.test.ts` passing. _Result: 3471 pass, 11 skip, 4 fail — the 4 are `src/hub/credential-context.test.ts` cases that fail identically on pristine `upstream/main` in this Hub-managed environment (see CLAUDE.md on projected Git/SSH wrappers); untouched by this change._
- [x] 6.2 Run `bun test:e2e tests/e2e/chat-touch.e2e.ts` and verify every case passes, then run the neighbouring chat and touch suites for regressions. _Result: chat-touch 18/18; chat-touch + touch-scroll + mobile + chat-requests + chat-panels + chat + chat-follow-stability serially: 119 passed, 2 failed — both in `chat-requests.e2e.ts › Allow always asks for confirmation`, reproduced identically with all `src/` changes stashed, so pre-existing on this macOS environment._
- [x] 6.3 Verify on a physical iPhone in Safari and in the installed PWA: (#410) focus the composer, background the app with the keyboard open, return → the chat surface fills the screen with no keyboard-sized strip and the reading position is unchanged; (#354/#382) with the task, subagent, and background-task lists populated and expanded and the keyboard open → the composer and send control stay visible; (#354) answer a question with a free-form response → the pill does not cover the field or the Answer/Cancel controls and the card does not scroll away; (#354) move the caret inside the answer field → the conversation does not scroll; (#354/#382, answering) focus a question's free-form field with the keyboard open → the header stays visible, the composer and the three tracks pass under the keyboard's edge rather than disappearing and are back above it once the keyboard dismisses, the pill and the prompt rail are hidden, the field and Answer/Reject sit above the keyboard, the page does not zoom, and tapping Answer submits without the layout jumping first; (#354, pill) with the request card on screen the pill is hidden and it returns when the card is scrolled away. Record device, OS version, and build badge. This is the only confirmation of the two device-only hypotheses — that iOS suppresses the visual-viewport `resize` across a background transition, and that the fixed-top surface and the pan form a feedback loop — so do not close this task from browser tests. _Partial result: verified by the maintainer on an iPhone 13 over the tailnet dev hub (Safari) against the working tree of 2026-09-20 — background return, panned keyboard, track budget, answering mode (chrome under the keyboard, field held on the keyboard's edge, no zoom, clean Answer tap), pill yield and caret handling all reported working; iOS version and build badge not recorded. Reopened during review: installed-PWA verification is not recorded, the required OS/build evidence is missing, and the review fixes need device re-verification. Preserve the Safari evidence; do not infer the remaining checks from automated browser tests._ _Closed by the maintainer at merge (2026-09-21, #415 as `7287ac0`): the Safari evidence stands as the device acceptance for this change; installed-PWA acceptance and OS/build recording are not tracked further here._
- [x] 6.4 Run `openspec validate keep-touch-chat-true-to-visible-viewport --strict` and confirm the implemented behavior matches both delta requirements.
- [x] 6.5 Record per-issue reproduction evidence on the latest stable tag before choosing release-note visibility. The original assertion that all three reproduce in `v0.7.0` is withdrawn: see `release-evidence.md` for source inspection and missing runtime/device evidence. Keep the truthful `fix(chat)` title; use a Release Please override if the correction proves unreleased-only. PR #415 links #410, #382, and #354 and records the desktop/terminal viewport lifecycle follow-ups, but its earlier blanket stable-impact statement must be corrected with user-approved PR text before merge. _Closed by the maintainer at merge (2026-09-21): release-note visibility decided as a visible `fix(chat)` entry without an override — #354 predates the `v0.7.0` tag and the touch viewport code had not changed between 2026-09-05 and the tag; no tagged-build device reproduction was recorded._

- [x] 6.6 Fix the review's combined resize/pan correction gap by comparing the actual visual height as well as the applied surface height; regress a simultaneous height/offset change while answering and preserve pure-pan suppression. _Result: new viewport test failed with 1 correction instead of 2 before the fix; focused viewport/scroll/question-form/UI wrapper suite passes (42 tests)._
- [x] 6.7 Fix the review's hold lifecycle gaps: follow the actual focused answer element on direct transfer, release the old owners, and restore retained answer focus on foreground return after cancellation. Cover parent and drill-down without renewing a gesture-released hold on ordinary corrections. _Result: both new UI lifecycle cases failed before the fix and passed afterward; the isolated UI suite passes (16 tests)._
- [x] 6.8 Correct the review's overstated device-acceptance record: reopen 6.3, retain the recorded Safari result, and name the missing installed-PWA and OS/build evidence. Device acceptance remains open rather than being claimed from automation.
- [x] 6.9 Run the added touch browser regressions for direct focus transfer, retained focus after foreground return, and simultaneous resize/pan; run the surrounding touch suite in Chromium and WebKit, typecheck, and strict OpenSpec validation. Record results and any limitations here. _Result: `bun test src/chat` — 1049 pass, 5 skip, 0 fail; isolated UI cases — 16 pass. `bun run test:e2e tests/e2e/chat-touch.e2e.ts tests/e2e/chat-follow-stability.e2e.ts --workers=1` — 41 pass (25 Chromium touch cases plus 16 Chromium/WebKit follow-stability cases; rerun with a longer command timeout after the initial 120-second runner limit). `bun run test:e2e tests/e2e/chat-touch.e2e.ts --workers=1 --browser=webkit` — 23 pass, 2 fail: configuration-trigger focus restoration and a 0.8125px history-anchor difference. Both failures reproduce identically with `ui.ts` and `viewport.ts` restored to pre-review-fix HEAD `0770d82`; the three new browser cases fail against that code and pass with the fixes. `bun run typecheck`, strict OpenSpec validation and `git diff --check` pass. No physical-device verification performed; 6.3 remains open._

- [x] 6.10 Address [PR #415's follow-up review](https://github.com/tjakobsson/uatu/pull/415#issuecomment-5752034047): scope editing-time pan suppression to touch so a simultaneous pan and touch-to-desktop switch cannot skip its first correction; regress that transition and the return to touch. Document the request observer's existing document lifetime (pagehide is not disposal) and the question-form-specific answer extent rather than introducing a speculative teardown or permission editor. Verify observer retention on pagehide and disconnection when no request remains, chat unit tests, typecheck, and relevant touch browser cases. _Result: new mode-switch test failed before the guard (1 correction rather than 2) and passes afterward; viewport tests 13 pass, isolated UI tests 16 pass; `bun test src/chat` — 1050 pass, 5 skip, 0 fail. Focused touch browser cases (mode switching, request-pill visibility, parent/drill-down answer hold, combined resize/pan) — 5/5 Chromium and 5/5 WebKit. Typecheck, strict OpenSpec validation, and diff checks pass. Device acceptance remains open in 6.3._

- [x] 6.11 Address the [maintainer's eight findings](https://github.com/tjakobsson/uatu/pull/415#pullrequestreview-5261601457): reconcile detached WebKit answer focus after paint; restore pre-answer following without overriding an unpinned reader; let keyboard padding outrank the pill reservation; leave pinch zoom browser-owned; preserve actual holds across automatic snapshots but not explicit navigation/gestures; let choice-control drags pause; release a hold on Latest; and replace unsupported stable-release assertions with per-issue evidence. Preserve real WebKit touch activation by guarding mousedown rather than cancelling pointerdown. Regress parent and child answering/rejection/removal, follow after response and refresh, zoom, and drill-down clearance; run unit, browser and type checks and record results. _Result: targeted red/green tests reproduced stale editing, missing refresh holds, lost following, retained Latest holds, choice-drag suppression, zoom misclassification and hidden child answer controls. An additional regression protects explicit A→B navigation from reholding A and overwriting B's saved paused anchor. `bun test src/chat` — 1066 pass, 5 skip, 0 fail; Chromium touch suite — 35 pass; WebKit touch suite — 33 pass, the same 2 previously baselined failures (configuration-trigger focus restoration and 0.8125px history-anchor precision); Chromium/WebKit desktop/touch follow-stability suite — 16 pass. Typecheck, strict OpenSpec validation and diff checks pass. Radio setup now waits for restored tab-bar geometry before the next check rather than racing the newly resumed follow behavior; the real input-transfer and touch-tap assertions remain intact. Stable/device evidence and approved PR-body correction remain open in 6.3/6.5._

## 7. Answering a request on touch (#354, #382 — field test)

- [x] 7.1 Derive `data-chat-answering` on `<html>` in `syncEditingFocus` (`src/chat/ui.ts` ~3365) when the focused text control sits inside `.chat-request` / `[data-question-form]`, cleared with the rest of the editing state on blur. Verify with cases in `src/chat/ui.test.ts`: set on focusing a question's custom-answer input, cleared on blur, and not set when the composer takes focus.
- [x] 7.2 Add the touch rules under `html[data-ui-mode="touch"][data-chat-answering]` in `src/styles.css`, beside the existing keyboard rules (~8790s, NOT the tab-bar rules at ~8020–8045): hide `.chat-header`, the five pinned tracks (`#chat-task-list`, `#chat-subagents`, `#chat-background-tasks`, `#chat-reverted`, `#chat-queue`, summaries included), `#chat-composer` and `#chat-requests-jump`. Verify the transcript takes the whole visible band while answering and the chrome returns on blur.
- [x] 7.3 Keep the focused control inside the visible viewport once the keyboard geometry settles, driven through `CoordinatedScrollOwner` (`src/chat/coordinated-scroll.ts`) rather than a raw `scrollTop` or `scrollIntoView` on the nested timeline. Its current surface is `request()`, `flush()`, `beforeMutation()`, `latest()`, `pause()`, `cancel()`, `dispose()` — there is no reveal seam, so add a minimal `reveal(element)` to the owner. Trigger it when the answering state begins and again when the viewport height changes while answering. Verify with a case in `src/chat/coordinated-scroll.test.ts` that the reveal is the only position write and that it leaves the anchor consistent.
- [x] 7.4 Give `[data-question-primary]` and `[data-question-reject]` the touch `pointerdown` `preventDefault()` the send button already has (`src/chat/ui.ts` ~3373), and the permission card's choices too where they can blur an answer field. Verify a touch tap on Answer submits without the field blurring and reflowing the chrome under the finger first.
- [x] 7.5 Add `html[data-ui-mode="touch"] .chat-question-custom-editor input { font-size: 16px; }` to `src/styles.css` beside the composer and configuration-dialog rules (~7995). Verify iOS Safari does not zoom the page when the custom-answer input takes focus.
- [x] 7.6 Gate the pill on intersection in `syncOutstandingRequests` (`src/chat/ui.ts` ~861): observe the current target card with an `IntersectionObserver` rooted at the timeline and keep `requestsJump.hidden` true while it intersects, leaving the count unchanged when the pill does show; disconnect the observer when the target changes or the surface is torn down. Verify with a case in `src/chat/ui.test.ts` using a stubbed `IntersectionObserver`.
- [x] 7.7 Add browser coverage in `tests/e2e/chat-touch.e2e.ts`: with the keyboard open and a question's free-form field focused, the header, the tracks, the composer and the pill are hidden, the field's bottom and the Answer control's bottom are within the visual viewport height, and all of the chrome is back after blur; the custom input's computed `font-size` is `16px`; and the pill is hidden while the request card is on screen and shown once the card is scrolled away.
- [x] 7.8 Rerun 6.1 and 6.2 (`bun test`, then `bun test:e2e tests/e2e/chat-touch.e2e.ts` plus the neighbouring chat and touch suites) and record the results inline in the same style as 6.1 and 6.2. _Result: `bun test` alone: 3473 pass, 11 skip, 4 fail (the same `credential-context` environment cases as 6.1). chat-touch 22/22. Isolated reruns after aligning the pill cases to D12: chat-requests + chat-inventory-presentation + chat-panels 36 pass, 2 fail (the pre-existing order-dependent `Allow always asks for confirmation` pair); mobile + chat-inventory-presentation 31/31. A full-suite overlap with `bun test` hung on a spawned `serve` child and an agent-written `testdata/watch-docs/audit-report.md` broke the document-count cases until removed — both environmental._
- [x] 7.9 Hold the revealed position while answering (D13): add `hold(element)` / `release()` to `CoordinatedScrollOwner` (`src/chat/coordinated-scroll.ts`) so a scroll the owner did not write is undone on the next coordinated frame by re-running the reveal, with held scrolls taking no part in the pause/unpin path, and `cancel()`/`dispose()` clearing the hold; wire it in `src/chat/ui.ts` `syncEditingFocus` — hold the focused field in its owning timeline on the transition into answering, release both owners on the transition out — leaving the viewport correction path on `reveal`. Verify with a case in `src/chat/coordinated-scroll.test.ts` (a foreign `scroll` is corrected back on the next frame with one write, the owner's own write starts no loop, and after `release()` a foreign scroll is left alone), a case in `src/chat/ui.test.ts` spying `hold`/`release` on the prototype (held on focusin, released on focusout), and a case in `tests/e2e/chat-touch.e2e.ts` driving the platform autoscroll — `scrollTop` moved with a `scroll` event while the field is focused returns, and stays put once the field is blurred.
- [x] 7.10 Replace the hide-everything answering layout with the keyboard covering the chrome (design D10 v2). Keep `--chat-visual-height` at the layout viewport height (`window.innerHeight`) while `data-chat-answering` is set (`src/chat/viewport.ts`), with `--chat-visual-top` still following the pan. Clamp `revealDelta`'s band bottom to the visual viewport's bottom (`visualViewport.offsetTop + height`) rather than the timeline's own in `src/chat/coordinated-scroll.ts`, leaving the scroll padding in place. In `src/styles.css` drop the `.chat-header`, pinned-track, `#chat-composer` and `.chat-latest` hide rules and the `#chat-timeline` padding release from the answering block, keep the prompt rail and `#chat-requests-jump` hidden, and add a `height` transition (~250 ms) on the touch chat surface so the bottom chrome slides under an already-open keyboard. Cover it in `src/chat/viewport.test.ts` (answering → layout height, top still panned), `src/chat/coordinated-scroll.test.ts` (the reveal clamps to the visual viewport rather than the scroller's bottom), and `tests/e2e/chat-touch.e2e.ts` (with the keyboard open and the field focused: the composer's top is at or below the visual viewport's bottom, the header is visible, the field's bottom is within the visual viewport, and the chrome is back above the former keyboard edge after blur — including a WebKit run of the answering cases). Verify no chrome is `display: none` while answering and that `bun test src/chat/viewport.test.ts src/chat/coordinated-scroll.test.ts src/chat/ui.test.ts` and `bun test:e2e tests/e2e/chat-touch.e2e.ts` pass.
- [x] 7.11 Align the held answer extent to the bottom of the visible band (D14). Extend `reveal(element, options?)` and `hold(element, options?)` in `src/chat/coordinated-scroll.ts` with `{ extent?: HTMLElement; align?: "nearest" | "end" }`: `revealDelta` measures the union of `element` and `extent`, `align: "end"` targets the union's bottom at the band's bottom (a negative delta scrolls back up), falling back to aligning the union's top when it is taller than the band, and `align: "nearest"` keeps today's minimal move as the default; `hold()` passes `align: "end"`; keep the clamp to the scroller's scroll range and the ±0.5 px write guard. In `src/chat/ui.ts` resolve the extent as the answering form's `.chat-request-actions` row (`element.closest("form[data-question-form]")?.querySelector(".chat-request-actions")`) in one helper shared by `holdInOwningTimeline` and the viewport controller's re-reveal path (`revealInOwningTimeline`), so the field and its Answer/Reject controls are placed together. Focus the custom editor with `input.focus({ preventScroll: true })` in `syncCustomEditor` (`src/chat/question-form.ts`), since the owner performs the reveal itself. Verify with a case in `src/chat/coordinated-scroll.test.ts` (a field already inside the band but high in it is moved down until the extent's bottom sits at the band's bottom, and the move is clamped when the scroller cannot go further), a case in `src/chat/question-form.test.ts` (focus receives `preventScroll`), and assertions in `tests/e2e/chat-touch.e2e.ts` in both "puts the chrome under the keyboard…" and "holds still…" that the Answer control's bottom sits within the scroll padding of the visible viewport's bottom, so no strip wider than that padding is left above the keyboard. Verify `bun test src/chat/coordinated-scroll.test.ts src/chat/ui.test.ts src/chat/question-form.test.ts src/chat/viewport.test.ts`, `bunx tsc --noEmit -p .`, and `bunx playwright test tests/e2e/chat-touch.e2e.ts --workers=1` all pass, including a WebKit run of the answering cases.
