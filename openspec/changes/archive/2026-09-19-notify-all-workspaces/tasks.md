## 1. Store and coordinator

- [x] 1.1 Add `allWorkspaces: boolean` to `NotificationPreferences`/`NotificationDevice` in `src/hub/notification-store.ts`; `load()` defaults a missing field to `false`. Verify with a store test that a pre-change device record loads as `allWorkspaces: false` and round-trips unchanged.
- [x] 1.2 Give `HubNotifications` a `workspaces: () => string[]` option and a `covers(device, ws)` helper; route `eligible`, `active`, `refresh`'s observer set, and `forget`/removal through it. Verify with a `notifications.test.ts` case where an all-workspaces device and a one-workspace device share a hub: a question in an unselected workspace reaches only the all-workspaces device, and an all-workspaces device on an empty registry still reports `active: true`.
- [x] 1.3 Extend `preferences()` and `enroll()`: accept optional `allWorkspaces` (default `false`), keep the sent `workspaceIds`, settle every running registered workspace when it is on, and stamp `since` for every registered workspace with the existing carry-over rules. Verify with tests that (a) an all-workspaces enrollment with two running workspaces waits for both feeds and stamps cutoffs no earlier than their positions, (b) a stalled feed is named in the 503 and no record is written, (c) a re-save with `allWorkspaces: false` restores eligibility to the stored list only.
- [x] 1.4 In `refresh()`, stamp cutoffs for workspaces newly registered since an all-workspaces device's last stamp (one mutation, before that workspace's observer starts) and drop `since` entries plus pending deliveries for ids no longer registered. Verify with tests that a workspace registered after enrollment and then started delivers a new question but not one already pending before the stamp, and that unregistering a covered workspace discards its pending delivery while another workspace's delivery still sends.
- [x] 1.5 Wire `workspaces` in `src/hub/main.ts` from `registry.list()` and include `allWorkspaces` in `state()`. Verify `bun test src/hub` passes and `GET /api/hub/notifications` in `hub.integration.test.ts` returns the field for a new and a legacy device.

## 2. Hub API contract

- [x] 2.1 Update `api/openapi.yaml`: optional `allWorkspaces` on `NotificationEnrollment`, required `allWorkspaces` on the `NotificationState` device object, `info.version` 7.20.0-experimental, `info.summary`, `x-uatu-revisions.hubApiRevision: 7`; bump `api/contract.json` and `HUB_API_REVISION` in `src/shared/version.ts`; add the `## Hub 7 / Workspace 20 - Unreleased` changelog section (`Compatibility: breaking (Hub)`, Migration paragraph naming the regenerate step and the downgrade behaviour). Verify `bun run api:validate`, `bun run test:api`, and `src/shared/api-revisions.test.ts` pass, and the CI "Enforce compatibility" step run locally against `origin/main` reports the change as breaking (Hub) only.

## 3. Enrollment dialog

- [x] 3.1 Add the "All workspaces, including ones added later" checkbox at the top of the workspace fieldset in `src/pwa/notification-client.js`; disable the per-workspace checkboxes while it is checked without clearing them; send `allWorkspaces` on save; reflect the stored mode on open; word the saved/enabled status per mode. Verify by hand in `bun run dev` from both the dashboard and a workspace sidebar: the control defaults off, the current workspace is still preselected in a workspace, toggling disables and re-enables the list with ticks intact, and reopening after save shows the saved mode.
- [x] 3.2 Extend `tests/e2e/notifications.e2e.ts` with a device that saves All workspaces, reloads, sees the mode preserved with the list disabled, registers a new workspace through the hub API, and confirms the device state still reports `allWorkspaces: true` without the new id in `workspaceIds`; capture the dialog in both modes through `tests/e2e/evidence.ts`. Verify the test passes in `bun test:e2e --grep notifications`.

## 4. Documentation

- [x] 4.1 Update `README.md` "Web notifications" and `docs/SELF-HOSTING.md` "Web Push notifications" to describe the All workspaces option, replace "Newly registered workspaces are not subscribed automatically" with the per-mode rule, and note the downgrade behaviour in the rollback paragraph. Verify by reading the rendered sections and that no other doc still states the old absolute rule (`grep -rn "not subscribed automatically" README.md docs`).
