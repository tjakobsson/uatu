# OpenCode SDK coverage

<!-- Generated by `bun run coverage:agents`. Do not edit: the freshness test in scripts/agent-coverage.test.ts rejects hand edits. -->

uatu treats OpenCode as a first-class agent. The aim is to use what its SDK offers, not the subset every agent has in common. This page lists every message type, content block or part, and tool the installed SDK declares, and what uatu does with each today. The gaps are the work still to do. None of them means a feature is unwanted.

Generated against 1.x through `@opencode-ai/sdk` 1.18.30 · 2.x through `@opencode/client` 2.0.13 (vocabulary from `@opencode/schema` 2.0.13).

**Gaps: 30** (unhandled or behavior-missing) · dedicated 112 · generic 0 · ignored 85 · unhandled 30 · behavior-missing 0

Each entry is classified by running uatu's own normalizers and renderers on a stub of it, so the page cannot drift from what the code does. The hand-written part is small: why a type is ignored, and which features show up but do not work yet.

| State | Meaning |
| --- | --- |
| dedicated | used: uatu has purpose-built rendering or behavior for it |
| generic | shown, but only through the generic tool row; not a gap, and a candidate for a surface of its own |
| ignored | deliberately not shown, for the stated reason |
| unhandled | not used yet, and no decision made: a gap |
| behavior-missing | shown, but uatu does not yet deliver what it does: a gap; the reason says what fails |

<!-- agent-coverage:since:start -->
## Since the previous generation

First generation: there is no previous matrix to compare against.
<!-- agent-coverage:since:end -->

## 1.x events <a id="axis-1.x-events"></a>

Source: `@opencode-ai/sdk` `v2/gen/types.gen.d.ts` `Event` (the client the 1.x provider imports).

| Name | State | Reason |
| --- | --- | --- |
| `catalog.updated` | ignored | The model catalog changed; uatu fetches models through the API when it needs them. |
| `command.executed` | unhandled |  |
| `file.edited` | unhandled |  |
| `file.watcher.updated` | ignored | Project state on the server; uatu watches the workspace itself. |
| `global.disposed` | ignored | Server connection lifecycle; uatu tracks the server through its own connection. |
| `installation.update-available` | unhandled |  |
| `installation.updated` | ignored | The OpenCode installation's own lifecycle; not conversation activity. |
| `integration.connection.updated` | ignored | Server configuration changed; not conversation activity. |
| `integration.updated` | ignored | Server configuration changed; not conversation activity. |
| `lsp.updated` | ignored | Language-server state on the server; not conversation activity. |
| `mcp.browser.open.failed` | ignored | MCP server bookkeeping; MCP tool calls themselves arrive as tool events. |
| `mcp.tools.changed` | ignored | MCP server bookkeeping; MCP tool calls themselves arrive as tool events. |
| `message.part.delta` | dedicated |  |
| `message.part.removed` | dedicated |  |
| `message.part.updated` | dedicated |  |
| `message.removed` | dedicated |  |
| `message.updated` | dedicated |  |
| `models-dev.refreshed` | unhandled |  |
| `permission.asked` | dedicated |  |
| `permission.replied` | dedicated |  |
| `permission.v2.asked` | dedicated |  |
| `permission.v2.replied` | dedicated |  |
| `plugin.added` | ignored | Server configuration changed; not conversation activity. |
| `project.directories.updated` | ignored | Project state on the server; uatu watches the workspace itself. |
| `project.updated` | ignored | Project state on the server; uatu watches the workspace itself. |
| `pty.created` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.next.shell.* events. |
| `pty.deleted` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.next.shell.* events. |
| `pty.exited` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.next.shell.* events. |
| `pty.updated` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.next.shell.* events. |
| `question.asked` | dedicated |  |
| `question.rejected` | dedicated |  |
| `question.replied` | dedicated |  |
| `question.v2.asked` | dedicated |  |
| `question.v2.rejected` | dedicated |  |
| `question.v2.replied` | dedicated |  |
| `reference.updated` | ignored | Project state on the server; uatu watches the workspace itself. |
| `server.connected` | ignored | Server connection lifecycle; uatu tracks the server through its own connection. |
| `server.instance.disposed` | ignored | Server connection lifecycle; uatu tracks the server through its own connection. |
| `session.compacted` | unhandled |  |
| `session.created` | dedicated |  |
| `session.deleted` | dedicated |  |
| `session.diff` | unhandled |  |
| `session.error` | dedicated |  |
| `session.idle` | dedicated |  |
| `session.next.agent.switched` | dedicated |  |
| `session.next.compaction.delta` | ignored | Streams compaction progress; the started and ended events bracket the one compaction row. |
| `session.next.compaction.ended` | dedicated |  |
| `session.next.compaction.started` | dedicated |  |
| `session.next.context.updated` | dedicated |  |
| `session.next.model.switched` | dedicated |  |
| `session.next.moved` | unhandled |  |
| `session.next.prompt.admitted` | dedicated |  |
| `session.next.prompted` | dedicated |  |
| `session.next.reasoning.delta` | dedicated |  |
| `session.next.reasoning.ended` | dedicated |  |
| `session.next.reasoning.started` | dedicated |  |
| `session.next.retried` | dedicated |  |
| `session.next.revert.cleared` | dedicated |  |
| `session.next.revert.committed` | dedicated |  |
| `session.next.revert.staged` | dedicated |  |
| `session.next.shell.ended` | dedicated |  |
| `session.next.shell.started` | dedicated |  |
| `session.next.step.ended` | dedicated |  |
| `session.next.step.failed` | dedicated |  |
| `session.next.step.started` | unhandled |  |
| `session.next.synthetic` | dedicated |  |
| `session.next.text.delta` | dedicated |  |
| `session.next.text.ended` | dedicated |  |
| `session.next.text.started` | dedicated |  |
| `session.next.tool.called` | dedicated |  |
| `session.next.tool.failed` | dedicated |  |
| `session.next.tool.input.delta` | ignored | Streams the tool's input; the call event carries it whole. |
| `session.next.tool.input.ended` | ignored | Streams the tool's input; the call event carries it whole. |
| `session.next.tool.input.started` | ignored | Streams the tool's input; the call event carries it whole. |
| `session.next.tool.progress` | dedicated |  |
| `session.next.tool.success` | dedicated |  |
| `session.status` | dedicated |  |
| `session.updated` | dedicated |  |
| `todo.updated` | unhandled |  |
| `tui.command.execute` | ignored | Instructions for OpenCode's own terminal UI, which uatu is not. |
| `tui.prompt.append` | ignored | Instructions for OpenCode's own terminal UI, which uatu is not. |
| `tui.session.select` | ignored | Instructions for OpenCode's own terminal UI, which uatu is not. |
| `tui.toast.show` | ignored | Instructions for OpenCode's own terminal UI, which uatu is not. |
| `vcs.branch.updated` | ignored | Project state on the server; uatu watches the workspace itself. |
| `workspace.failed` | ignored | Project state on the server; uatu watches the workspace itself. |
| `workspace.ready` | ignored | Project state on the server; uatu watches the workspace itself. |
| `workspace.status` | ignored | Project state on the server; uatu watches the workspace itself. |
| `worktree.failed` | ignored | Project state on the server; uatu watches the workspace itself. |
| `worktree.ready` | ignored | Project state on the server; uatu watches the workspace itself. |

## 1.x message parts <a id="axis-1.x-parts"></a>

Source: `@opencode-ai/sdk` `v2/gen/types.gen.d.ts` `Part`.

| Name | State | Reason |
| --- | --- | --- |
| `agent` | unhandled |  |
| `compaction` | unhandled |  |
| `file` | unhandled |  |
| `patch` | unhandled |  |
| `reasoning` | dedicated |  |
| `retry` | unhandled |  |
| `snapshot` | ignored | An internal git tree hash; nothing to show. |
| `step-finish` | ignored | Its cost and tokens are restated on the message's message.updated, which the usage carrier reads. |
| `step-start` | ignored | Marks a step boundary; carries only a snapshot hash. |
| `subtask` | unhandled |  |
| `text` | dedicated |  |
| `tool` | dedicated |  |

## 2.x events <a id="axis-2.x-events"></a>

Source: `@opencode/schema` `event-manifest.d.ts` and `session-event.d.ts`.

| Name | State | Reason |
| --- | --- | --- |
| `agent.updated` | ignored | Server configuration changed; uatu reads agents through the API. |
| `command.executed` | unhandled |  |
| `command.updated` | ignored | Server configuration changed; uatu reads commands through the API. |
| `config.updated` | ignored | Server configuration changed; not conversation activity. |
| `credential.switched` | ignored | Server configuration changed; not conversation activity. |
| `credential.updated` | ignored | Server configuration changed; not conversation activity. |
| `file.edited` | unhandled |  |
| `filesystem.changed` | ignored | Project state on the server; uatu watches the workspace itself. |
| `form.cancelled` | dedicated |  |
| `form.created` | dedicated |  |
| `form.replied` | dedicated |  |
| `global.disposed` | unhandled |  |
| `installation.update-available` | ignored | The OpenCode installation's own lifecycle; not conversation activity. |
| `installation.updated` | ignored | The OpenCode installation's own lifecycle; not conversation activity. |
| `integration.updated` | ignored | Server configuration changed; not conversation activity. |
| `location.shutdown` | ignored | Server connection lifecycle; uatu tracks the server through its own connection. |
| `lsp.updated` | unhandled |  |
| `mcp.resources.changed` | ignored | MCP server bookkeeping; MCP tool calls themselves arrive as tool events. |
| `mcp.status.changed` | ignored | MCP server bookkeeping; MCP tool calls themselves arrive as tool events. |
| `mcp.tools.changed` | unhandled |  |
| `model.updated` | ignored | Server configuration changed; uatu reads models through the API. |
| `models-dev.refreshed` | ignored | The model catalog refreshed; uatu fetches models through the API when it needs them. |
| `permission.asked` | dedicated |  |
| `permission.replied` | dedicated |  |
| `persistent-pty.added` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.shell.* events. |
| `persistent-pty.removed` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.shell.* events. |
| `plugin.updated` | ignored | Server configuration changed; not conversation activity. |
| `project.updated` | ignored | Project state on the server; uatu watches the workspace itself. |
| `provider.updated` | ignored | Server configuration changed; uatu reads models through the API. |
| `pty.created` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.shell.* events. |
| `pty.deleted` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.shell.* events. |
| `pty.exited` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.shell.* events. |
| `pty.updated` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.shell.* events. |
| `reference.updated` | ignored | Project state on the server; uatu watches the workspace itself. |
| `server.connected` | ignored | Server connection lifecycle; uatu tracks the server through its own connection. |
| `session.agent.selected` | dedicated |  |
| `session.compacted` | unhandled |  |
| `session.compaction.delta` | ignored | Streams compaction progress; the started and ended events bracket the one compaction row. |
| `session.compaction.ended` | dedicated |  |
| `session.compaction.failed` | dedicated |  |
| `session.compaction.started` | dedicated |  |
| `session.created` | dedicated |  |
| `session.deleted` | dedicated |  |
| `session.execution.failed` | dedicated |  |
| `session.execution.interrupted` | dedicated |  |
| `session.execution.started` | dedicated |  |
| `session.execution.succeeded` | dedicated |  |
| `session.forked` | unhandled |  |
| `session.idle` | dedicated |  |
| `session.inbox.cancelled` | unhandled |  |
| `session.inbox.delivered` | ignored | Delivery bookkeeping for a prompt already shown when session.inbox.enqueued accepted it. |
| `session.inbox.delivery.changed` | ignored | Delivery bookkeeping for a prompt already shown when session.inbox.enqueued accepted it. |
| `session.inbox.enqueued` | dedicated |  |
| `session.instructions.updated` | ignored | Which instruction files the session loaded; uatu does not present the agent's system context. |
| `session.message.content.updated` | dedicated |  |
| `session.model.selected` | dedicated |  |
| `session.moved` | unhandled |  |
| `session.permissions` | ignored | The session's standing permission rules; each request arrives as permission.asked. |
| `session.reasoning.delta` | dedicated |  |
| `session.reasoning.ended` | dedicated |  |
| `session.reasoning.started` | dedicated |  |
| `session.renamed` | dedicated |  |
| `session.retry.scheduled` | dedicated |  |
| `session.revert.cleared` | dedicated |  |
| `session.revert.committed` | dedicated |  |
| `session.revert.staged` | dedicated |  |
| `session.shell.ended` | dedicated |  |
| `session.shell.started` | dedicated |  |
| `session.skill.activated` | unhandled |  |
| `session.status` | dedicated |  |
| `session.step.ended` | dedicated |  |
| `session.step.failed` | dedicated |  |
| `session.step.started` | dedicated |  |
| `session.step.streamed` | ignored | Streams step progress; the step's started and ended events carry what is shown. |
| `session.synthetic` | dedicated |  |
| `session.text.delta` | dedicated |  |
| `session.text.ended` | dedicated |  |
| `session.text.started` | dedicated |  |
| `session.tool.called` | dedicated |  |
| `session.tool.failed` | dedicated |  |
| `session.tool.input.delta` | ignored | Streams the tool's input; the call event carries it whole. |
| `session.tool.input.ended` | ignored | Streams the tool's input; the call event carries it whole. |
| `session.tool.input.started` | ignored | Streams the tool's input; the call event carries it whole. |
| `session.tool.progress` | dedicated |  |
| `session.tool.success` | dedicated |  |
| `session.usage.recorded` | ignored | The session's running total; per-message figures ride session.step.ended. |
| `session.usage.updated` | ignored | The session's running total; per-message figures ride session.step.ended. |
| `session.viewed` | ignored | A client marked the session read; nothing happened in the conversation. |
| `shell.created` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.shell.* events. |
| `shell.deleted` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.shell.* events. |
| `shell.exited` | ignored | Terminal sessions on the OpenCode server, not the conversation's; the agent's shell runs arrive as session.shell.* events. |
| `skill.updated` | ignored | Server configuration changed; not conversation activity. |
| `tui.command.execute` | ignored | Instructions for OpenCode's own terminal UI, which uatu is not. |
| `tui.prompt.append` | ignored | Instructions for OpenCode's own terminal UI, which uatu is not. |
| `tui.session.select` | ignored | Instructions for OpenCode's own terminal UI, which uatu is not. |
| `tui.toast.show` | ignored | Instructions for OpenCode's own terminal UI, which uatu is not. |
| `vcs.branch.updated` | ignored | Project state on the server; uatu watches the workspace itself. |
| `websearch.updated` | ignored | Server configuration changed; not conversation activity. |
| `workspace.failed` | unhandled |  |
| `workspace.ready` | unhandled |  |
| `workspace.status` | unhandled |  |
| `worktree.failed` | unhandled |  |
| `worktree.ready` | unhandled |  |
| `worktree.resolved` | ignored | Project state on the server; uatu watches the workspace itself. |
| `worktree.updated` | ignored | Project state on the server; uatu watches the workspace itself. |

## 2.x assistant content <a id="axis-2.x-parts"></a>

Source: `@opencode/schema` `session-message.d.ts` `AssistantContent`.

| Name | State | Reason |
| --- | --- | --- |
| `reasoning` | dedicated |  |
| `text` | dedicated |  |
| `tool` | dedicated |  |

## Tools <a id="axis-tools"></a>

Source: not enumerated by either SDK.

Neither OpenCode SDK enumerates tool names: a tool is typed as a plain string. This lists only the names uatu's renderer treats as dedicated; every other tool name renders through the generic tool row.

| Name | State | Renders as | Reason |
| --- | --- | --- | --- |
| `agent` | dedicated | agent row |  |
| `apply_patch` | dedicated | patch row |  |
| `bash` | dedicated | command row |  |
| `command` | dedicated | command row |  |
| `edit` | dedicated | edit row |  |
| `fetch` | dedicated | fetch row |  |
| `glob` | dedicated | search row |  |
| `grep` | dedicated | search row |  |
| `list` | dedicated | search row |  |
| `question` | dedicated | question row |  |
| `read` | dedicated | read row |  |
| `shell` | dedicated | command row |  |
| `skill` | dedicated | skill row |  |
| `task` | dedicated | agent row |  |
| `terminal` | dedicated | command row |  |
| `todowrite` | dedicated | todo row |  |
| `webfetch` | dedicated | fetch row |  |
| `write` | dedicated | write row |  |
