# Pin this project to public npm regardless of any user-level `~/.npmrc`
# configuration (e.g. a Cloudsmith mirror). Without this, `bun add` on a
# developer machine writes mirror-specific URLs into `bun.lock` and CI —
# which has no auth for the mirror — fails with 401s on every dependency.
registry=https://registry.npmjs.org/
