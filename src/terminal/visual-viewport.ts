// Compatibility entry for the terminal's existing consumers and tests.
// The observer is shared with the desktop workspace's viewport owner.
export { createVisualViewportSizer, type VisualViewportLike, type VisualViewportMetrics, type VisualViewportSizer } from "../shared/visual-viewport";
