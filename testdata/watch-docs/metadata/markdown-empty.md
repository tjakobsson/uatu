# Plain Document

This Markdown document has no frontmatter. The metadata card is omitted
entirely so the body looks identical to how Markdown rendered before
metadata support landed.

A leading `---` later in the body still works as a thematic break:

---

The line above is rendered as `<hr />`, not parsed as frontmatter.
